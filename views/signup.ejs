<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="form.css">
    <link rel="stylesheet" href="header.css">
</head>
<body>

    <%- include('components/header') %>

    <main class="container">
         <h1 class="header-page-title">Sign Up</h1>
        <section class="form-card">

                <form action="/signup" method="post" novalidate>
                <div class="form-grid">
                    <div class="form-group">
                        <label for="firstname">First Name <span class="req">*</span></label>
                        <input class="input" type="text" name="firstname" id="firstname" value="<%= (body && body.firstname) || "" %>">
                        <% if(locals.errorsMap && locals.errorsMap.firstname) {%>
                            <div class="field-error"><%=locals.errorsMap.firstname.msg%></div>
                        <%}%>
                    </div>

                    <div class="form-group">
                        <label for="lastname">Last Name <span class="req">*</span></label>
                        <input class="input" type="text" name="lastname" id="lastname" value="<%= (body && body.lastname) || "" %>">
                        <% if(locals.errorsMap && locals.errorsMap.lastname) {%>
                            <div class="field-error"><%=locals.errorsMap.lastname.msg%></div>
                        <%}%>
                    </div>

                    <div class="form-group full">
                        <label for="username">Username <span class="req">*</span></label>
                        <input class="input" type="text" name="username" id="username" value="<%= (body && body.username) || "" %>">
                        <% if(locals.errorsMap && locals.errorsMap.username) {%>
                            <div class="field-error"><%=locals.errorsMap.username.msg%></div>
                        <%}%>
                    </div>

                    <div class="form-group">
                        <label for="password">Password <span class="req">*</span></label>
                        <input class="input" type="password" name="password" id="password">
                        <% if(locals.errorsMap && locals.errorsMap.password) {%>
                            <div class="field-error"><%=locals.errorsMap.password.msg%></div>
                        <%}%>
                    </div>

                    <div class="form-group">
                        <label for="password">Confirm password <span class="req">*</span></label>
                        <input class="input" type="password" name="confirm_password" id="confirm-password">
                        <% if(locals.errorsMap && locals.errorsMap.confirm_password) {%>
                            <div class="field-error"><%=locals.errorsMap.confirm_password.msg%></div>
                        <%}%>
                    </div>
            </div>
                <div class="form-actions"><button type="submit" class="btn">Submit</button></div>
            </form>
        </section>
    </main>
</body>
</html>